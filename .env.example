# tcpwatch (Electron) – local env
# Copy to `.env` and fill in values.
# Do NOT commit `.env` (it is gitignored).

# Required for the Captures → right-click → Analyze feature
ANTHROPIC_API_KEY=''

# Optional: override Claude model
# TCPWATCH_CLAUDE_MODEL=claude-opus-4-5-20251101
# TCPWATCH_CLAUDE_MODEL=claude-sonnet-4-5-20250929

# Optional: override mcpcap executable path (otherwise uses repo .mcp.json)
# TCPWATCH_MCPCAP_BIN=/abs/path/to/mcpcap
