# tcpwatch-app-v0.2.5

## Changes

- **Default snap length increased to 65535** — The default capture snap length is now 65535 bytes (full packet) instead of 200 bytes, ensuring complete packet payloads are captured by default.
