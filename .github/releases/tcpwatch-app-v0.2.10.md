# tcpwatch-app-v0.2.10

## Improvements

- **Descriptive admin password dialog** — When the auto-updater needs administrator privileges to replace a root-owned `.app` bundle, the macOS password dialog now explains why: "tcpwatch needs administrator privileges to replace the current application bundle with the downloaded update."
