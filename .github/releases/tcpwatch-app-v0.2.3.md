# tcpwatch-app-v0.2.3

## Features

- **Clickable capture file paths** — The capture file path shown on both the Connections page ("Capture file: ...") and the Captures page ("Capture: ...") is now a clickable link that opens the file in Wireshark.
